{
  "name": "compositor",
  "version": "1.0.0",
  "private": true,
  "description": "Compositor - sentence craft, drilled on your own drafts. One entry a day, in a single self-contained HTML file.",
  "scripts": {
    "build": "node lib/build.js",
    "test": "node test/engine.test.js && node test/site.test.js",
    "check": "npm run build && npm test"
  }
}
