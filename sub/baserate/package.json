{
  "name": "baserate",
  "version": "1.0.0",
  "private": true,
  "description": "Base Rate - whether a number means anything. One entry a day, plus a forecast book scored on Brier.",
  "scripts": {
    "build": "node lib/build.js",
    "test": "node test/engine.test.js && node test/site.test.js",
    "check": "npm run build && npm test"
  }
}
