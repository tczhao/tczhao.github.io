{
  "name": "leastauthority",
  "version": "1.0.0",
  "private": true,
  "description": "Least Authority - systems that act on untrusted input. One entry a day, with expiry dates on the fast-moving half.",
  "scripts": {
    "build": "node lib/build.js",
    "test": "node test/engine.test.js && node test/site.test.js",
    "check": "npm run build && npm test"
  }
}
