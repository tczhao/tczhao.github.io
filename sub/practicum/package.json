{
  "name": "practicum",
  "version": "1.0.0",
  "private": true,
  "description": "A daily logbook for management practice - one entry a day, with spaced review",
  "scripts": {
    "build": "node build.js",
    "test": "node test/engine.test.js && node test/assets.test.js",
    "check": "npm run build && npm test"
  }
}
