{
  "name": "maillard",
  "version": "1.0.0",
  "private": true,
  "description": "Maillard - the chemistry under the cooking. One entry a day, with a kitchen log instead of a review deck.",
  "scripts": {
    "build": "node ../_shared/lib/build.js",
    "test": "node ../_shared/test/engine.test.js && node ../_shared/test/site.test.js",
    "check": "npm run build && npm test"
  }
}
